import User from "./models/User";
import Product from "js/models/Product";
import { formatCurrency, formatDate } from './utils/helpers.js';
import { validateEmail } from './utils/validators.js';
import config from './config/settings.js';
console.log(`app: ${config.appName} v${config.version}`);

// User তৈরি
let user = new User("safaat", "safaat@gmail.com");
user.displayInfo();

// Product তৈরি
let product = new Product("mobile", 15000);
product.display();

// Helpers ব্যবহার
console.log(formatCurrency(15000));
console.log(formatDate(new Date()));

// Validation
console.log(validateEmail("safaat@gmail.com")); 